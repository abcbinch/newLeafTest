내가 아는 쿠키는, 
사용자의 정보를 쿠키의 형태로 저장해서,
로그인할 때 편하도록 만드는 그런 기능이다.

그런데 여기서 쿠키 값을 설정해서 저장한다는 것은 
뭘 의미할까?
내가 왜 쿠키 값을 지정해야 하지?
내가 설정해서 저장하면 쿠키 값은 고정되어 있을 텐데.
그럼 로그인한 유저의 정보를 저장하는 건 어떻게 하는 거지?
그건 사람들마다 다 다르니까 유동적으로 변할 텐데.

쿠키는 json 문자열로 저장되나?